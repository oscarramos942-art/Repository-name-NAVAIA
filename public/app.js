const state={token:localStorage.getItem("navaia_token"),user:null,businesses:[],page:"dashboard"};

const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const money=n=>new Intl.NumberFormat("es-DO",{style:"currency",currency:"DOP",maximumFractionDigits:2}).format(Number(n||0));
async function api(path,opt={}){const r=await fetch(path,{...opt,headers:{"Content-Type":"application/json",...(state.token?{Authorization:"Bearer "+state.token}:{}),...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(r.status===401){logout();throw Error("Sesión expirada");}if(!r.ok)throw Error(d.error||"Error de servidor");return d}
function app(){return document.getElementById("app")}
function loginView(){app().innerHTML=`<div class="login"><div class="card"><div class="brand">NAVAIA</div><div class="sub">Panel central para tus negocios</div><div id="err"></div><div class="field"><label>Correo</label><input id="email" type="email" autocomplete="email"></div><div class="field"><label>Contraseña</label><input id="password" type="password" autocomplete="current-password"></div><button class="primary" onclick="login()">Entrar</button><p class="muted">Primer acceso: usa el endpoint de registro o completa el registro desde la administración.</p></div></div>`}
async function login(){try{const d=await api("/auth/login",{method:"POST",body:JSON.stringify({email:document.getElementById("email").value,password:document.getElementById("password").value})});state.token=d.token;localStorage.setItem("navaia_token",state.token);await boot()}catch(e){document.getElementById("err").innerHTML='<div class="error">'+esc(e.message)+'</div>'}}
function logout(){localStorage.removeItem("navaia_token");state.token=null;state.user=null;loginView()}
async function boot(){try{state.user=(await api("/auth/me")).user;state.businesses=await api("/api/businesses");render()}catch(e){logout()}}
function shell(content){app().innerHTML=`<div class="layout"><aside class="side"><div class="logo">N</div><div class="nav">
<button class="${state.page==="dashboard"?"active":""}" onclick="go('dashboard')">🏠 Dashboard</button>
<button class="${state.page==="businesses"?"active":""}" onclick="go('businesses')">🏢 Negocios</button>
<button class="${state.page==="customers"?"active":""}" onclick="go('customers')">👥 Clientes</button>
<button class="${state.page==="inventory"?"active":""}" onclick="go('inventory')">📦 Inventario</button>
<button class="${state.page==="quotes"?"active":""}" onclick="go('quotes')">🧾 Cotizaciones</button>
<button class="${state.page==="invoices"?"active":""}" onclick="go('invoices')">💳 Facturas</button>
<button class="${state.page==="finance"?"active":""}" onclick="go('finance')">💰 Finanzas</button>
<button class="${state.page==="projects"?"active":""}" onclick="go('projects')">📐 Proyectos</button>
<button class="${state.page==="poultry"?"active":""}" onclick="go('poultry')">🐔 Avícola</button>
<button class="${state.page==="ai"?"active":""}" onclick="go('ai')">🤖 NAVAIA IA</button>
</div></aside><main class="main"><div class="top"><div><div class="muted">Bienvenido</div><h1>${esc(state.user?.name||"Usuario")}</h1></div><button class="ghost" onclick="logout()">Cerrar sesión</button></div>${content}</main></div>`}
function go(p){state.page=p;render()}
async function render(){if(!state.token)return loginView();try{if(state.page==="dashboard")return dashboard();if(state.page==="businesses")return businesses();if(state.page==="customers")return customers();if(state.page==="inventory")return inventory();if(state.page==="quotes")return quotes();if(state.page==="invoices")return invoices();if(state.page==="finance")return finance();if(state.page==="projects")return projects();if(state.page==="poultry")return poultry();if(state.page==="ai")return aiView()}catch(e){shell('<div class="error">'+esc(e.message)+'</div>')}}
async function dashboard(){const d=await api("/api/dashboard");shell(`<div class="grid"><div class="stat"><div class="muted">Negocios</div><div class="n">${d.businesses}</div></div><div class="stat"><div class="muted">Clientes</div><div class="n">${d.customers}</div></div><div class="stat"><div class="muted">Productos</div><div class="n">${d.products}</div></div><div class="stat"><div class="muted">Proyectos activos</div><div class="n">${d.activeProjects}</div></div><div class="stat"><div class="muted">Ingresos del mes</div><div class="n">${money(d.monthIncome)}</div></div><div class="stat"><div class="muted">Gastos del mes</div><div class="n">${money(d.monthExpense)}</div></div><div class="stat"><div class="muted">Aves registradas</div><div class="n">${d.birds}</div></div><div class="stat"><div class="muted">Huevos registrados</div><div class="n">${d.eggs}</div></div></div><div class="section panel"><h2>Control general</h2><p class="muted">NAVAIA ya tiene una base central para tus negocios. Usa el menú para registrar operaciones reales.</p></div>`)}
function selectBusiness(id="business_id"){return '<select id="'+id+'"><option value="">Seleccionar negocio</option>'+state.businesses.map(b=>'<option value="'+b.id+'">'+esc(b.name)+'</option>').join("")+'</select>'}
function formModal(title,fields,save){const fs=fields.map(f=>'<div class="field"><label>'+esc(f.label)+'</label>'+((f.type||"text")==="select"?f.html:'<input id="'+f.id+'" type="'+(f.type||"text")+'" value="'+esc(f.value||"")+'">')+'</div>').join("");document.body.insertAdjacentHTML("beforeend",'<div class="modal" id="modal"><div class="card"><h2>'+esc(title)+'</h2>'+fs+'<div class="toolbar"><button onclick="'+save+'">Guardar</button><button class="ghost" onclick="document.getElementById(\'modal\').remove()">Cancelar</button></div></div></div>')}
function businesses(){shell('<div class="toolbar"><button onclick="newBusiness()">+ Negocio</button></div><div class="grid">'+state.businesses.map(b=>'<div class="panel"><h3>'+esc(b.name)+'</h3><div class="muted">'+esc(b.type)+'</div><p>'+esc(b.description||"")+'</p></div>').join("")+'</div>')}
function newBusiness(){formModal("Nuevo negocio",[{id:"name",label:"Nombre"},{id:"type",label:"Tipo"},{id:"description",label:"Descripción"}],"saveBusiness()")}
async function saveBusiness(){try{await api("/api/businesses",{method:"POST",body:JSON.stringify({name:val("name"),type:val("type"),description:val("description")})});document.getElementById("modal").remove();state.businesses=await api("/api/businesses");render()}catch(e){alert(e.message)}}
function customers(){api("/api/customers").then(rows=>{shell('<div class="toolbar"><button onclick="newCustomer()">+ Cliente</button></div><div class="panel tablewrap"><table class="table"><tr><th>Nombre</th><th>Teléfono</th><th>Negocio</th><th>Correo</th></tr>'+rows.map(x=>'<tr><td>'+esc(x.name)+'</td><td>'+esc(x.phone)+'</td><td>'+esc(x.business_name)+'</td><td>'+esc(x.email)+'</td></tr>').join("")+'</table></div>')})}
function newCustomer(){formModal("Nuevo cliente",[{id:"name",label:"Nombre"},{id:"phone",label:"Teléfono"},{id:"email",label:"Correo"},{id:"address",label:"Dirección"},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"saveCustomer()")}
async function saveCustomer(){try{await api("/api/customers",{method:"POST",body:JSON.stringify({name:val("name"),phone:val("phone"),email:val("email"),address:val("address"),business_id:val("business_id")||null})});document.getElementById("modal").remove();customers()}catch(e){alert(e.message)}}
function inventory(){api("/api/products").then(rows=>{shell('<div class="toolbar"><button onclick="newProduct()">+ Producto</button></div><div class="panel tablewrap"><table class="table"><tr><th>Producto</th><th>Negocio</th><th>Stock</th><th>Costo</th><th>Precio</th></tr>'+rows.map(x=>'<tr><td>'+esc(x.name)+'</td><td>'+esc(x.business_name)+'</td><td>'+x.stock+' '+esc(x.unit)+'</td><td>'+money(x.cost)+'</td><td>'+money(x.price)+'</td></tr>').join("")+'</table></div>')})}
function newProduct(){formModal("Nuevo producto",[{id:"name",label:"Nombre"},{id:"sku",label:"SKU"},{id:"category",label:"Categoría"},{id:"unit",label:"Unidad",value:"unidad"},{id:"stock",label:"Stock",type:"number"},{id:"min_stock",label:"Stock mínimo",type:"number"},{id:"cost",label:"Costo",type:"number"},{id:"price",label:"Precio",type:"number"},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"saveProduct()")}
async function saveProduct(){try{await api("/api/products",{method:"POST",body:JSON.stringify({name:val("name"),sku:val("sku"),category:val("category"),unit:val("unit"),stock:val("stock"),min_stock:val("min_stock"),cost:val("cost"),price:val("price"),business_id:val("business_id")||null})});document.getElementById("modal").remove();inventory()}catch(e){alert(e.message)}}
function quotes(){api("/api/quotations").then(rows=>shell('<div class="toolbar"><button onclick="newQuote()">+ Cotización</button></div><div class="panel tablewrap"><table class="table"><tr><th>Número</th><th>Cliente</th><th>Negocio</th><th>Estado</th><th>Total</th></tr>'+rows.map(x=>'<tr><td>'+esc(x.number)+'</td><td>'+esc(x.customer_name)+'</td><td>'+esc(x.business_name)+'</td><td>'+esc(x.status)+'</td><td>'+money(x.total)+'</td></tr>').join("")+'</table></div>'))}
function newQuote(){formModal("Nueva cotización",[{id:"number",label:"Número (opcional)"},{id:"subtotal",label:"Subtotal",type:"number"},{id:"tax",label:"Impuesto",type:"number"},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"saveQuote()")}
async function saveQuote(){try{await api("/api/quotations",{method:"POST",body:JSON.stringify({number:val("number"),business_id:val("business_id")||null,tax:val("tax"),items:[{description:"Servicio / producto",quantity:1,unit_price:val("subtotal")}]})});document.getElementById("modal").remove();quotes()}catch(e){alert(e.message)}}
function invoices(){api("/api/invoices").then(rows=>shell('<div class="toolbar"><button onclick="newInvoice()">+ Factura</button></div><div class="panel tablewrap"><table class="table"><tr><th>Número</th><th>Cliente</th><th>Estado</th><th>Total</th><th>Fecha</th></tr>'+rows.map(x=>'<tr><td>'+esc(x.number)+'</td><td>'+esc(x.customer_name)+'</td><td>'+esc(x.status)+'</td><td>'+money(x.total)+'</td><td>'+esc(x.created_at?.slice(0,10))+'</td></tr>').join("")+'</table></div>'))}
function newInvoice(){formModal("Nueva factura",[{id:"number",label:"Número (opcional)"},{id:"amount",label:"Monto",type:"number"},{id:"tax",label:"Impuesto",type:"number"},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"saveInvoice()")}
async function saveInvoice(){try{await api("/api/invoices",{method:"POST",body:JSON.stringify({number:val("number"),business_id:val("business_id")||null,tax:val("tax"),items:[{description:"Venta",quantity:1,unit_price:val("amount")}]})});document.getElementById("modal").remove();invoices()}catch(e){alert(e.message)}}
function finance(){api("/api/transactions").then(rows=>shell('<div class="toolbar"><button onclick="newTransaction()">+ Movimiento</button></div><div class="panel tablewrap"><table class="table"><tr><th>Fecha</th><th>Tipo</th><th>Descripción</th><th>Negocio</th><th>Monto</th></tr>'+rows.map(x=>'<tr><td>'+esc(x.transaction_date)+'</td><td>'+esc(x.type)+'</td><td>'+esc(x.description)+'</td><td>'+esc(x.business_name)+'</td><td>'+money(x.amount)+'</td></tr>').join("")+'</table></div>'))}
function newTransaction(){formModal("Nuevo movimiento",[{id:"description",label:"Descripción"},{id:"amount",label:"Monto",type:"number"},{id:"category",label:"Categoría"},{id:"type",label:"Tipo",type:"select",html:'<select id="type"><option value="income">Ingreso</option><option value="expense">Gasto</option></select>'},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"saveTransaction()")}
async function saveTransaction(){try{await api("/api/transactions",{method:"POST",body:JSON.stringify({description:val("description"),amount:val("amount"),category:val("category"),type:val("type"),business_id:val("business_id")||null})});document.getElementById("modal").remove();finance()}catch(e){alert(e.message)}}
function projects(){api("/api/projects").then(rows=>shell('<div class="toolbar"><button onclick="newProject()">+ Proyecto</button></div><div class="grid">'+rows.map(x=>'<div class="panel"><h3>'+esc(x.name)+'</h3><div>'+esc(x.business_name||"Sin negocio")+'</div><div class="muted">'+esc(x.status)+' · '+money(x.budget)+'</div></div>').join("")+'</div>'))}
function newProject(){formModal("Nuevo proyecto",[{id:"name",label:"Nombre"},{id:"budget",label:"Presupuesto",type:"number"},{id:"start_date",label:"Inicio",type:"date"},{id:"due_date",label:"Entrega",type:"date"},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"saveProject()")}
async function saveProject(){try{await api("/api/projects",{method:"POST",body:JSON.stringify({name:val("name"),budget:val("budget"),start_date:val("start_date"),due_date:val("due_date"),business_id:val("business_id")||null})});document.getElementById("modal").remove();projects()}catch(e){alert(e.message)}}
function poultry(){api("/api/poultry").then(rows=>shell('<div class="toolbar"><button onclick="newPoultry()">+ Lote avícola</button></div><div class="panel tablewrap"><table class="table"><tr><th>Lote</th><th>Tipo</th><th>Aves</th><th>Alimento kg</th><th>Huevos</th><th>Mortalidad</th></tr>'+rows.map(x=>'<tr><td>'+esc(x.name)+'</td><td>'+esc(x.bird_type)+'</td><td>'+x.current_birds+'</td><td>'+x.feed_kg+'</td><td>'+x.eggs_count+'</td><td>'+x.mortality+'</td></tr>').join("")+'</table></div>'))}
function newPoultry(){formModal("Nuevo lote avícola",[{id:"name",label:"Nombre del lote"},{id:"bird_type",label:"Tipo",value:"ponedoras"},{id:"initial_birds",label:"Aves iniciales",type:"number"},{id:"feed_kg",label:"Alimento kg",type:"number"},{id:"eggs_count",label:"Huevos",type:"number"},{id:"mortality",label:"Mortalidad",type:"number"},{id:"business_id",label:"Negocio",type:"select",html:selectBusiness()}],"savePoultry()")}
async function savePoultry(){try{await api("/api/poultry",{method:"POST",body:JSON.stringify({name:val("name"),bird_type:val("bird_type"),initial_birds:val("initial_birds"),feed_kg:val("feed_kg"),eggs_count:val("eggs_count"),mortality:val("mortality"),business_id:val("business_id")||null})});document.getElementById("modal").remove();poultry()}catch(e){alert(e.message)}}
function ai(){shell('<div class="panel ai"><h2>NAVAIA IA</h2><p class="muted">Consulta el estado general de tus negocios.</p><textarea id="aiq" placeholder="Ej.: ¿Cómo están mis negocios este mes?"></textarea><button class="primary" onclick="askAI()">Preguntar a NAVAIA</button><div id="answer" class="answer hide"></div></div>')}
function aiView(){ai()}
async function askAI(){const a=document.getElementById("answer");a.classList.remove("hide");a.textContent="Consultando...";try{a.textContent=(await api("/ai/chat",{method:"POST",body:JSON.stringify({message:document.getElementById("aiq").value})})).answer}catch(e){a.textContent=e.message}}
const val=id=>document.getElementById(id)?.value||"";
if(state.token)boot();else loginView();
